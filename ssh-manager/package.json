{
  "name": "ssh-manager",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "cert": "cd cert && mkcert -install && mkcert localhost 127.0.0.1",
    "logs": "tail -f logs/audit-*.log | jq ."
  },
  "dependencies": {}
}
